# Server–Client Encryption System

## 📝 Описание
Проектът представлява многомодулна Java система тип **client–server**, реализираща криптиране и декриптиране на номера на банкови карти при спазване на зададени ограничения за достъп, брой опити и валидност на данните.

Системата е разработена с цел демонстрация на:
- Клиент–сървърна архитектура
- Работа със сокети и многонишков сървър
- Криптиране на данни
- Контрол на достъпа (Authentication & Authorization)
- Работа с XML и логване на събития

---

## 🏗 Архитектура

Проектът е разделен на три основни модула:

1. **`common/`** – Обща логика, независима от мрежата:
   - `CardCipher`: Криптиране и декриптиране на 16-цифрени номера.
   - `LuhnValidator`: Проверка за валидност на номера на карта чрез алгоритъма на Luhn.
2. **`server/`** – Сървърна функционалност:
   - Многонишков TCP сървър (`ServerSocket`, `ClientHandler`).
   - Управление на потребители и права (ENCRYPT / DECRYPT / ADMIN).
   - Логване на събития във файл и съхранение на потребители в XML.
3. **`client/`** – Клиентска част (реализира връзка със сървъра чрез текстов протокол).

---

## 🔌 Клиентски модул
Клиентската част на системата е реализирана като конзолно Java приложение, което осъществява TCP връзка със сървъра и комуникира с него чрез дефинирания текстов протокол.

Клиентът поддържа следните операции:
- **Автентикация:** чрез потребителско име и парола.
- **Криптиране:** на валиден 16-цифрен номер на карта.
- **Декриптиране:** на получена криптограма.
- **Сесия:** коректно приключване на сесията.

Клиентът служи за функционално тестване на сървърната логика и демонстрира пълната client–server комуникация в системата.

### ▶ Примерна клиентска сесия:
```text
LOGIN admin admin
ENC 4563960122001999
DEC <получената_криптограма>
QUIT
```
---

## 🖥 Административен интерфейс

Проектът включва и административен графичен потребителски интерфейс,
реализиран с JavaFX и пакетиран като отделен JAR файл.
Интерфейсът е предназначен за управление на потребители и данни и е
проектиран като самостоятелен, преизползваем компонент.

---

## 🔐 Криптиране и Валидация

Използван е **полиялфавитен substitution cipher върху цифри**:
- Работи върху 16-цифрени номера.
- Използва случаен `baseShift` (1–15).
- При всеки следващ опит отместването се увеличава.
- Криптирането е симетрично (възможно е декриптиране).

**Преди криптиране номерът задължително преминава през:**
- Проверка за дължина (16 цифри).
- Luhn валидация.
- Проверка за начална цифра (3, 4, 5 или 6).

---

## 👥 Потребители и Права

Потребителите се зареждат от XML файл (`users.xml`). 

### Налични права:
- `ENCRYPT`: Право за криптиране.
- `DECRYPT`: Право за декриптиране.
- `ADMIN`: Административни права.

> **Default потребител:** Ако XML файлът липсва, се създава:  
> **User:** `admin` | **Pass:** `admin` | **Rights:** `ADMIN, ENCRYPT, DECRYPT`

---

## 📡 Текстов протокол (Client ↔ Server)

### Команди от клиента:
- `LOGIN <username> <password>`
- `ENC <16-digit-card-number>`
- `DEC <16-digit-cryptogram>`
- `QUIT`

### Отговори от сървъра:
- `OK <message>`
- `ERR <code> <message>`

---

## 🚀 Стартиране на сървъра
Сървърът се стартира и управлява изцяло чрез графичния административен интерфейс (Admin UI).
Стъпки:
1. Стартирайте класа `AdminApp`.
2. Ще се отвори Admin Panel (JavaFX).
3. Натиснете бутона Start, за да стартирате сървъра.
4. Сървърът започва да слуша на конфигурирания порт (по подразбиране 80).
5. Броят на активните клиенти и логовете се обновяват автоматично в реално време.

При натискане на Stop:
- всички свързани клиенти се уведомяват със съобщение
  `ERR SERVER STOPPED`
- връзките им се затварят коректно.
  
---

## 🧪 Ръчно тестване на клиент (през терминал)
Клиенти могат да се свържат чрез TCP сокет, например:
```bash
nc localhost 80
